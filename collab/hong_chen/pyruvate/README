
---------------------------------------------------------------


Usual commands to run:

mm; % the iMM904 model

[qMat, fluxMat] = SSPyruvateAll(mm, 'glucose', 0.1, 20, 0.3);
[qMat, fluxMat] = SSPyruvateAll(mm, 'pyruvate', 0.1, 28, 0.3);

Oddly, gurobi had trouble on one of the pyruvate simulations; GLPK worked fine.
